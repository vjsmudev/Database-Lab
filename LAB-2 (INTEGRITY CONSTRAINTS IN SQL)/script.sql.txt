create table employee(
  empno numeric(5) primary key,
  empname varchar(20) not null, 
  gender char(1) not null check (gender = 'm' or gender = 'f'),
  address varchar(20) not null,
  salary numeric(8)  not null,
  Dno numeric(5)
);

create table department(
    deptno numeric(5) primary key,
    deptname varchar(20) unique,
    location varchar(20)
);

alter table employee add constraint FK_Dno foreign key  (Dno) references department(deptno); 

insert into department values(1,'CS','manipal');
insert into department values(2,'MECH','mangalore');
insert into department values(3,'ELECTRONICS','hyd');

insert into employee values(1,'john','m','hyd',14000,2);
insert into employee values(2,'surya','m','manipal',45000,1);
insert into employee values(3,'alex','f','us',45000,3);

select* from employee;
select* from department; 

insert into department values(4,'COMMS','maa');
insert into employee values(1,'josh','m','sin',40321);
insert into employee values(2,'morgan','m','uk',23200);

alter table employee drop constraint FK_Dno;
alter table employee add constraint FK_Dno foreign key (Dno) references department (Dno) delete on cascade;

alter table employee modify salary = 10000;

